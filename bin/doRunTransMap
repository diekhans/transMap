#!/bin/sh -e

# clear path to avoid hitting kkhome
mach=$(uname -m)
export PATH="/hive/users/${USER}/opt/current/${mach}/bin:/cluster/bin/$mach:/usr/bin:/bin"
unset LD_LIBRARY_PATH
binDir=$(dirname $0)
exec $binDir/runTransMap "$@"
