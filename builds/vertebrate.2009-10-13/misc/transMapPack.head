#!/bin/bash
set -beEu -o pipefail

doPack() {
    local db=$1 tbl=$2
    local tblFile=/data/mysql/$db/$tbl
    echo packing "$tblFile.MYI" >&2
    myisampack -f $tblFile.MYI
    echo reindexing "$tblFile" >&2
    myisamchk -rq $tblFile
}

